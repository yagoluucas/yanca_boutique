---
import type { ProductContent } from '../../content/types';

interface Props {
  product: ProductContent;
}

const { product } = Astro.props;
---

<article class="product-card">
  <a
    class="product-card__image-link"
    href={product.link.href}
    target={product.link.external ? '_blank' : undefined}
    rel={product.link.external ? 'noopener noreferrer' : undefined}
    aria-label={`${product.link.label}: ${product.name}`}
  >
    <span class="product-card__badge">{product.badge}</span>
    <img
      src={product.image.src}
      alt={product.image.alt}
      width={product.image.width}
      height={product.image.height}
      loading="lazy"
      decoding="async"
    />
  </a>
  <div class="product-card__content">
    <p class="product-card__category">{product.category}</p>
    <h3>{product.name}</h3>
    <p class="product-card__description">{product.description}</p>
    <div class="product-card__buying">
      <p class="product-card__price">{product.price}</p>
      <p class="product-card__installment">{product.installment}</p>
    </div>
    <a
      class="text-link"
      href={product.link.href}
      target={product.link.external ? '_blank' : undefined}
      rel={product.link.external ? 'noopener noreferrer' : undefined}
    >
      {product.link.label}
      <span aria-hidden="true"> →</span>
      {product.link.external && <span class="sr-only"> (abre em uma nova aba)</span>}
    </a>
  </div>
</article>
